let firstNum = Number(prompt('enter number #1:'));
let secondNum = Number(prompt('enter number #2:'));

alert(`User entered ${firstNum} and ${secondNum}:
    ${firstNum} + ${secondNum} = ${firstNum + secondNum}
    ${firstNum} - ${secondNum} = ${firstNum - secondNum}
    ${firstNum} * ${secondNum} = ${firstNum * secondNum}
    ${firstNum} / ${secondNum} = ${firstNum / secondNum}`
);
